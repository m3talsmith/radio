syntax = "proto3";

package radio;

option go_package = "./radio";

service RadioAPI {
  rpc Station(stream Request) returns (stream Broadcast) {}
}

message Request {
  string name = 1;
}

message Broadcast {
  string name = 2;
}